const { ethers, upgrades } = require("hardhat");

async function main() {
//   const WorldV2 = await ethers.getContractFactory("WorldV2");
//   const instance = await upgrades.upgradeProxy(WORLD_ADDRESS, WorldV2);
  console.log("World upgraded");
}

main();